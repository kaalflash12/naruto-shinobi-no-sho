name: Canonical V3 Hardening

on:
  push:
    branches:
      - main
    paths:
      - tools/ATIVAR-CLOUDFLARE-LIVE-V3.ps1
      - ATIVAR-CLOUDFLARE-LIVE.ps1
      - .github/workflows/canonical-v3-hardening.yml
  pull_request:
    paths:
      - tools/ATIVAR-CLOUDFLARE-LIVE-V3.ps1
      - ATIVAR-CLOUDFLARE-LIVE.ps1
      - .github/workflows/canonical-v3-hardening.yml

permissions:
  contents: read

jobs:
  validate-canonical-v3:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Parse canonical V3 and regression fixture
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $path = 'tools/ATIVAR-CLOUDFLARE-LIVE-V3.ps1'
          $tokens = $null
          $errors = $null
          [void][System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path $path), [ref]$tokens, [ref]$errors)
          if ($errors.Count -gt 0) {
            $errors | ForEach-Object { Write-Error $_.Message }
            throw "Canonical V3 parse failed with $($errors.Count) error(s)."
          }
          powershell.exe -NoProfile -ExecutionPolicy Bypass -File $path -ValidateRunParsing
          if ($LASTEXITCODE -ne 0) { throw "Canonical V3 regression fixture failed with exit code $LASTEXITCODE." }

      - name: Enforce Cloudflare Atlas and portable-tool hardening contracts
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $canonical = Get-Content 'tools/ATIVAR-CLOUDFLARE-LIVE-V3.ps1' -Raw
          $required = @(
            "login --device | Out-Host",
            "auth login --force --skipConfig --noBrowser",
            "run watch `$RunId --repo `$Repo --exit-status | Out-Host",
            "workflow run backend-secret-presence.yml --repo `$Repo --ref main *> `$null",
            "workflow run final-readiness-orchestration.yml --repo `$Repo --ref main *> `$null",
            'function Test-AtlasAuthenticated([string]$Atlas)',
            'function Invoke-AtlasCli([string]$Atlas, [string[]]$Arguments, [switch]$AllowFailure)',
            'function Get-AtlasProvisioningPlan([string]$ProjectId, [string]$ClusterName, [string]$Username, [string]$Password)',
            'function Format-AtlasArgumentsForLog([string[]]$Arguments)',
            'function Protect-AtlasDiagnostic([string]$Text, [string[]]$Arguments)',
            "if (Test-AtlasAuthenticated `$Atlas) { return }",
            "if (-not (Test-AtlasAuthenticated `$Atlas)) { throw 'MongoDB Atlas CLI nao ficou autenticado.' }",
            "`$project = @(`$projects | Where-Object { ([string]`$_.name) -eq 'naruto-shinobi-no-sho' }) | Select-Object -First 1",
            "`$cluster = @(`$clusters | Where-Object { ([string]`$_.name) -eq 'shinobi-no-sho' }) | Select-Object -First 1",
            "CreateCluster = @('clusters','create',`$ClusterName,'--projectId',`$ProjectId,'--provider','AWS','--region','US_EAST_1','--tier','M0','--output','json')",
            "ListAccess = @('accessLists','list','--projectId',`$ProjectId,'--limit','500','--output','json')",
            "CreateAccess = @('accessLists','create','0.0.0.0/0','--type','cidrBlock','--projectId',`$ProjectId)",
            "UpdateUser = @('dbusers','update',`$Username,'--password',`$Password,'--projectId',`$ProjectId)",
            "CreateUser = @('dbusers','create','readWriteAnyDatabase','--username',`$Username,'--password',`$Password,'--projectId',`$ProjectId)",
            "DescribeCluster = @('clusters','describe',`$ClusterName,'--projectId',`$ProjectId,'--output','json')",
            "ConnectionString = @('clusters','connectionStrings','describe',`$ClusterName,'--projectId',`$ProjectId,'--output','json')",
            'Invoke-AtlasCli $atlas $plan.CreateCluster',
            'Invoke-AtlasCli $atlas $plan.ListAccess',
            'Invoke-AtlasCli $atlas $plan.UpdateUser -AllowFailure',
            'Invoke-AtlasCli $atlas $plan.DescribeCluster -AllowFailure',
            'Invoke-AtlasCli $atlas $plan.ConnectionString',
            'Cluster MongoDB Atlas nao ficou pronto. Estado final:',
            "`$finalReadinessPassed = `$false",
            "`$finalReadinessPassed = `$true",
            "`$temporaryCloudflareSecretWritten -and `$finalReadinessPassed",
            '[switch]$ValidatePortableTools',
            'PASS_PORTABLE_TOOL_LOCK_REUSE',
            '[switch]$ValidateAtlasProvisioning',
            'PASS_ATLAS_EXPLICIT_PROJECT_SCOPED_PROVISIONING',
            '<redacted>',
            'Reutilizando GitHub CLI portatil existente:',
            'Reutilizando Node.js portatil existente:',
            'Reutilizando MongoDB Atlas CLI portatil existente:',
            'shinobi-gh-portable-{0}',
            'shinobi-node-portable-{0}',
            'shinobi-atlas-portable-{0}',
            "`$ghAuthenticated = (`$LASTEXITCODE -eq 0)",
            "`$wranglerAuthenticated = (`$LASTEXITCODE -eq 0)"
          )
          foreach ($needle in $required) {
            if (-not $canonical.Contains($needle)) { throw "Canonical V3 hardening missing: $needle" }
          }

          $unsafe = @(
            '& $Atlas auth whoami *> $null',
            '& $Gh auth status --hostname github.com *> $null',
            "& `$Npx --yes 'wrangler@4.119.0' whoami *> `$null",
            '$project = $projects | Select-Object -First 1',
            '$cluster = $clusters | Select-Object -First 1',
            '& $atlas dbusers update',
            "`$root = Join-Path `$env:TEMP 'shinobi-gh-portable'",
            "`$root = Join-Path `$env:TEMP 'shinobi-node-portable'",
            "`$root = Join-Path `$env:TEMP 'shinobi-atlas-portable'",
            '  if ($temporaryCloudflareSecretWritten) {'
          )
          foreach ($needle in $unsafe) {
            if ($canonical.Contains($needle)) { throw "Unsafe canonical V3 contract remains: $needle" }
          }
          $setupNeedle = "@('" + "setup','--projectId'"
          if ($canonical.Contains($setupNeedle)) { throw 'Unsafe Atlas setup provisioning dependency remains.' }
          Write-Host 'PASS_CANONICAL_V3_CLOUDFLARE_ATLAS_PORTABLE_HARDENING'

      - name: Validate explicit Atlas provisioning on Windows
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/ATIVAR-CLOUDFLARE-LIVE-V3.ps1 -ValidateAtlasProvisioning
          if ($LASTEXITCODE -ne 0) { throw "Atlas explicit provisioning fixture failed with exit code $LASTEXITCODE." }

      - name: Reproduce locked portable tools on Windows
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/ATIVAR-CLOUDFLARE-LIVE-V3.ps1 -ValidatePortableTools
          if ($LASTEXITCODE -ne 0) { throw "Portable tool lock fixture failed with exit code $LASTEXITCODE." }

      - name: Verify root launcher executes canonical V3 directly
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $path = 'ATIVAR-CLOUDFLARE-LIVE.ps1'
          $tokens = $null
          $errors = $null
          [void][System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path $path), [ref]$tokens, [ref]$errors)
          if ($errors.Count -gt 0) {
            $errors | ForEach-Object { Write-Error $_.Message }
            throw "Root launcher parse failed with $($errors.Count) error(s)."
          }

          $launcher = Get-Content $path -Raw
          $required = @(
            'tools/ATIVAR-CLOUDFLARE-LIVE-V3.ps1',
            '[System.Management.Automation.Language.Parser]::ParseFile',
            'PASS_BOOTSTRAP_CANONICAL_V3_READY',
            '& powershell.exe -NoProfile -ExecutionPolicy Bypass -File $target -Repo $Repo'
          )
          foreach ($needle in $required) {
            if (-not $launcher.Contains($needle)) { throw "Root launcher contract missing: $needle" }
          }
          if ($launcher.Contains('function Replace-Required')) { throw 'Obsolete text-patching layer remains in root launcher.' }
          if ($launcher.Contains('Hotfix atlas-setup-output')) { throw 'Obsolete Atlas hotfix contract remains in root launcher.' }
          Write-Host 'PASS_CANONICAL_V3_ROOT_LAUNCHER_DIRECT'
